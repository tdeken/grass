dir_name:
  config: "{{Config}}"
  cmd: "{{Cmd}}"
  api: "{{Api}}"
  internal: "{{Internal}}"
  internal_api: "{{InternalApi}}"
  internal_service: "{{InternalService}}"
  internal_config: "{{InternalConfig}}"
  proto: "{{Proto}}"


swagger:
  code: "{{SwaggerCode}}"
  msg: "{{SwaggerMsg}}"
  data: "{{SwaggerData}}"